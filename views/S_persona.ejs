<!DOCTYPE html>
<html>
  <head>
    <title><%= form %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% include templates/header.ejs %>
    <h1><%= form %></h1>
    <p>Welcome to <%= form %></p>


    <form name="addStudent" action="/S_persona/addStudent" method="post">

    <% for(var exKey in campos.persona) { %>
      <div>
        <label for=<%= campos.persona[exKey] %>><%= campos.persona[exKey] %> :</label>
        <input type="text" id=<%= campos.persona[exKey] %> name=<%= campos.persona[exKey] %> />
      </div>
    <% } %>

    <div class="button">
        <button type="submit">Send your message</button>
    </div>

    </form> 

  </body>
</html>
